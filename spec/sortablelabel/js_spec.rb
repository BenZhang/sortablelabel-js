require "spec_helper"

RSpec.describe Sortablelabel::Js do
  it "has a version number" do
    expect(Sortablelabel::Js::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
